name: Node.js CI/CD on test-yml branch

on:
  push:
    branches: [ test-yml ]  # Runs only on this branch

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 🧾 Checkout code
        uses: actions/checkout@v3

      - name: ⚙️ Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: 📦 Install dependencies
        run: npm install

      - name: 🧪 Run tests
        run: echo "No real tests yet, but CI is working!"

      - name: 🚀 Dummy Deploy
        run: echo "Deploy command will go here later"
